#include <algorithm>
#include <iostream>
using namespace std;

int t;
int n, m;
int a[20][20];  // 20*20
int d[20][20];

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    cin >> t;
    for (int i = 0; i < t; ++i) {
        cin >> n >> m;
        for (int x = 0; x < n; ++x) {
            for (int y = 0; y < m; ++y) {
                cin >> arr[i][j];
            }
        }

        memcpy(d, a, sizeof(a));

        for (int)
    }

    return 0;
}
