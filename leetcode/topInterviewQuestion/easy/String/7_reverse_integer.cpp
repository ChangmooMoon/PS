#include <iostream>
#include <string>
using namespace std;

class Solution {
   public:
    int reverse(int x) {
        string s = to_string(x);
        for (int i = 0; i < s.size() / 2; i++) {
        }
    }
};

int main() {
    Solution s;
    int x = -02340000;
    cout << x << endl;
}