#include <bits/stdc++.h>
using namespace std;

int main() {
    multiset<int> mset;
    mset.insert(1);
    mset.insert(2);
    mset.insert(0);
    priority_queue<int> pq;
    pq.

    cout << *mset.begin() << endl;  // 0
    cout << *mset.rbegin() << endl  // 2
}
