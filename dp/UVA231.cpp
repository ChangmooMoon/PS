#include <iostream>
using namespace std;
#define FASTIO cin.tie(nullptr)->sync_with_stdio(false)

int main() {
	FASTIO;
	int t, n;
	vector<int> a;
	while(cin >> n) {
		if(n == -1) break;
		do {
			
		} while(n != -1)
	}

	return 0;
}
