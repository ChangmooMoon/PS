#include <iostream>
#include <vector>
using namespace std;
#define FASTIO cin.tie(nullptr)->sync_with_stdio(false)

int n, a, b;
vector<int> v;
int main() {
    FASTIO;
    cin >> n >> a >> b;
    if (a + b - 1 > n) {
        cout << -1;
        return 0;
    }

    

    return 0;
}
